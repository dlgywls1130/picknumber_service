import React from 'react';
import Header from '../components/Header';

function IndicatorNavbar(){
    const pageTitle = '통합표시기';
    return(
        <div>
            <Header title={pageTitle} /> 
        </div> 
    );
}

export default IndicatorNavbar