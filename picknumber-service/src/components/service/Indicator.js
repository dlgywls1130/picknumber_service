import React from 'react';
import Header from '../Header';

function Indicator(){
    const pageTitle = '통합표시기';
    return(
        <div>
            <Header title={pageTitle} /> 
        </div>
    );
}

export default Indicator