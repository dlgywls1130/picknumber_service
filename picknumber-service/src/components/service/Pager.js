import React from 'react';
import Header from '../Header';

function Pager(){
    const pageTitle = '호출기';
    return(
        <div>
            <Header title={pageTitle} /> 
        </div>
    );
}

export default Pager