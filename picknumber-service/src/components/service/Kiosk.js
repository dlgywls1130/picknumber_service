import React from 'react';
import Header from '../Header';

function Kiosk(){
    const pageTitle = '발권기';
    return(
        <div>
            <Header title={pageTitle} /> 
        </div> 
    );
}

export default Kiosk